mysql的事务隔离性有4个级别

依次为
    读未提交（A事务可以读到B事务未提交的数据）
    读提交（只有B事务提交后，A事务才能看到B的更改）
    可重复读（一个事务在开始和结束的时候，看到的视图是一致的）
    串行化（后面的事务需要等前面的事务结束才可以执行）



脏读  一个事务读到了另外一个事务未提交的数据（读未提交）

不可重复读  一个事务读取同一条记录两次，结果不一样（读未提交，读提交）

幻读 一个事务读取两次，得到的记录条数不一样（有insert发生）